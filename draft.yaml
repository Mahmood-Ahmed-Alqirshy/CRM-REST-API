models:
    Interest:
        id: id
        name: string
        relationships:
            belongsToMany: Deal, Contact

    Location:
        id: id
        name: string
        relationships:
            hasMany: Contact

    Tag:
        id: id
        name: string:255
        relationships:
            belongsToMany: Deal

    Deal:
        id: id
        heading: text
        description: text
        datetime: dateTime
        annual: boolean
        image: text
        relationships:
            belongsToMany: Interest, Tag

    Contact:
        id: id
        name: string
        phone: string:45 nullable unique
        facebook_id: unsignedInteger nullable unique
        instagram_id: string:30 nullable unique
        email: string nullable unique
        location_id: unsignedBigInteger foreign:Location.id
        birthday: date
        relationships:
            belongsToMany: Interest
            belongsTo: Location


